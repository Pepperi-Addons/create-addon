{
  "name": "test",
  "version": "0.0.0",
  "description": "",
  "main": "api.ts",
  "types": "",
  "files": [
    "build"
  ],
  "license": "Apache-2.0",
  "keywords": [],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "tsc",
    "build": "rollup -c rollup.config.js && rollup -c installation.rollup.config.js",
    "start": "tsc -w app.local.ts"
  },
  "devDependencies": {
    "@types/dotenv": "^8.2.0",
    "@types/node": "^10.0.3",
    "rollup": "^2.7.6",
    "rollup-plugin-typescript2": "^0.27.0",
    "ts-node": "^8.10.1",
    "typescript": "~3.8.0"
  },
  "dependencies": {
    "@pepperi-addons/debug-server": "0.0.19"
  }
}
